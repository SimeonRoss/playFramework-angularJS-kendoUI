describe('BrewingTools.controllers.recipe', function() {

	beforeEach(module('BrewingTools.controllers.recipe'));
	
	describe('AbvCalculator', function() {
		
	 	it("should be tested at some stage", inject(function (AbvCalculator) {

	 	}));

	});
	
});